<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Fotos tiradas pela câmera — armazenadas em filesDir/images/ -->
    <files-path name="images" path="images/" />
    <!-- Áudios gravados — armazenados em filesDir/audio/ -->
    <files-path name="audio" path="audio/" />
    <!-- Cache de imagens temporárias -->
    <cache-path name="cache_images" path="images/" />
</paths>
